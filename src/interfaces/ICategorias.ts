// quando o TS reconhece o formato de categoria.nome como 'never', fazendo ele reconhecer o que será passado
export default interface ICategoria {
    nome: string;
    ingredientes: string[];
    imagem: string;
}